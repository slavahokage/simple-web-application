<html>
<head>
    <meta charset="utf-8">
</head>
<body>
<ul>
    {% for flash in flashes('success') %}
        <li>
            {{ flash }}
        </li>
    {% endfor %}
</ul>
<h1>List of news</h1>
<ul>

</ul>
{% for new in news %}
    <form method="post" action="/news/delete">
        <li>{{ new.title }} - {{ new.description }}
            <button name="id" value="{{ new.id }}" type="submit">delete</button>
        </li>
    </form>
{% endfor %}
</ul>
</body>
</html>